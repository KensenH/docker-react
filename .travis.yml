sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t kensenh/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kensenh/docker-react npm run test